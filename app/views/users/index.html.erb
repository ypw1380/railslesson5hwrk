<h1>Below are the Users!</h1>

<% @users.each do |user| %>
  <div>
    <h3>Name: <%= user.fname + ' ' + user.lname %></h3>
    <h3>Email: <%= user.email %></h3>
  </div>

  <h6><%= link_to 'Delete User', user_path(user), method: :delete %></h6>
<% end %>


